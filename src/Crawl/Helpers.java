package Crawl;

public class Helpers 
{
	static int getGreatest(int one, int two)
	{
		return one > two ? one : two;
	}
	static int getSmallest(int one, int two)
	{
		return one > two ? two : one;
	}
}
